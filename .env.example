# Environment variables for SMTP email (Contact Form)
# Copy this file to .env.local and fill in your credentials

# --- SMTP Configuration ---
# For Gmail: use App Password (not your real password)
# Create App Password at: https://myaccount.google.com/apppasswords

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# If these are not set, the contact form will work in dev mode
# (logs to console instead of sending real emails)
